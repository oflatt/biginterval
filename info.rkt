#lang info

(define collection "biginterval")
(define version "0.1")

;; Packaging information

(define pkg-desc "A wrapper around MPFI")
(define pkg-authors
  '("Oliver Flatt"))
(define deps '("base"
               "math-lib"))
